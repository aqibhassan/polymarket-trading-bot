apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mvhe-audit-logs
  namespace: mvhe
  labels:
    app: mvhe
    component: trading-bot
    version: "0.1.0"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
